<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.miwok_translator2020_kt_petprojects">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ColorsActivity"
            android:label="Colors"></activity>
        <activity
            android:name=".FamiliActivities"
            android:label="Family Activities" />
        <activity
            android:name=".PhrasesActivity"
            android:label="Phrases" />
        <activity
            android:name=".NumbersActivity"
            android:label="Numbers" />
        <activity
            android:name=".MainActivity"
            android:label="Main Menue Miwok">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>